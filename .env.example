# Your Kudosity API key — Settings → API Settings in the Kudosity platform
KUDOSITY_API_KEY=your_api_key_here

# Optional: override the API base URL (defaults to the v2 messaging host)
# KUDOSITY_API_BASE_URL=https://api.transmitmessage.com
